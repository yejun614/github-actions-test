name: Dotnet Lint

run-name: Run Dotnet Lint(${{ github.event.number }})

on:
  pull_request:
  workflow_dispatch:
    inputs:
      number:
        description: 'Pull Request Number'
        required: true
        type: string

jobs:
  Lint:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    env:
      PR_NUMBER: ${{ github.event.inputs.number }}
      WORKSPACE: ${{ vars.WORKSPACE }}
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '7.0.x'
      - name: Dotnet Format
        run: |
          dotnet tool install -g dotnet-format
          dotnet-format ${WORKSPACE} --report dotnet-format-report.json
      - name: Check Report
        run: |
          cat dotnet-format-report.json
          rm dotnet-format-report.json
      - name: Git commit & push
        run: |
          git add .
          git diff
          git config --local user.email "yejun614@naver.com"
          git config --local user.name "Dotnet Format Bot (by YeJun, Jung)"
          git commit -m "[Style] dotnet format(${PR_NUMBER})"
          git push